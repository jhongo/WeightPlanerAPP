import 'package:flutter/material.dart';

class InformartionScreen extends StatelessWidget {
   
  const InformartionScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('InformartionScreen'),
      ),
    );
  }
}